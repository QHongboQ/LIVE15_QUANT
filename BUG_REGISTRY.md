# LIVE15 guarded regression index

This is a compact index. Executable tests and evidence documents remain the
source of detail.

| Guarded regression | Durable fact / status | Guard / evidence | Related decision |
| --- | --- | --- | --- |
| Pyth false worker-progress / stale masking | Failed or duplicate input must not advance meaningful worker progress. **GUARDED**; the proven masking behavior is fixed. | `tests/test_native_recorder.py`, `tests/test_runtime_ownership.py` | `docs/adr/0003-runtime-ownership.md` |
| Pyth exact-WTI upstream unavailability / feed-local isolation | Exact WTI has no authoritative replacement. The feed-local circuit breaker removes only that feed and reprobes; siblings continue and degraded health stays honest. **GUARDED / HISTORICAL ROOT CAUSE**. | PR #33/#37; `tests/test_native_recorder.py`, `tests/test_runtime_ownership.py` | `docs/adr/0003-runtime-ownership.md` |
| RuntimeSupervisor service ACL delegation | The reported missing Codex ACE was corrected and the service-control result persisted. **RESOLVED / GUARDED**; reopen only on new permission evidence. | PR #31; `runtime/control-center-acl-correction-uac-result.txt` in bounded runtime evidence | `docs/adr/0003-runtime-ownership.md` |
| DataGap sequential restart identity/provenance | Sequential restart reproduced a conflict because mutable recorder/session provenance entered logical gap identity. Identity and provenance are separated; semantic conflicts still fail closed. **GUARDED**. | PR #34; DataGap regression tests | `docs/continuous_recorder.md` |
| Kalshi 15m rollover sparse snapshot / concurrent recv | Exactly one omitted book side is normalized before SDK decoding; both omitted or malformed sides remain rejected. Rollover replaces the SDK session rather than calling its concurrent `update_subscription` receive path. **GUARDED**. | `tests/test_kalshi_gateway.py`, `tests/test_production_recorder_host.py`, `tests/test_recorder_market_data_provider.py` | `docs/kalshi-sdk-v12-migration.md` |
| Replay-baseline gap | Historical delta prefixes without a usable baseline isolate or wait; they are never replay-verified by invention. **GUARDED**. | `tests/test_ws_archive.py`, `tests/test_ws_retention.py` | `docs/adr/0002-research-data-authority.md` |
| Archive quarantine | Quarantined rows remain preserved and non-purgeable; FAILED and WAITING remain fail-closed. **GUARDED**. | `tests/test_ws_retention.py` | `docs/continuous_recorder.md` |
| Stale UI/runtime truth | A stale receipt must not override current service health; UI exposes stale telemetry rather than a false failure. **GUARDED**. | `tests/test_runtime_ownership.py`, Control Center runtime tests | `docs/adr/0003-runtime-ownership.md` |
| Intentional auxiliary health projection | Historical child PID/heartbeat must not turn `ON_DEMAND` or `PAUSED_BY_DESIGN` auxiliaries into false STALE failures; stale RUNNING remains strict. **GUARDED**. | PR #39; `tests/test_runtime_ownership.py` | `docs/adr/0003-runtime-ownership.md` |
| Dataset-history confusion | Dataset partitions do not define current research coverage or authorize holdout access. **GUARDED**. | `tests/test_research_data_authority.py` | `docs/adr/0002-research-data-authority.md` |
| Frozen-holdout accidental exposure | A broad local artifact search displayed frozen-holdout rows, then stopped immediately. **CONTAINED / REMEDIATION REQUIRED**: prior `UNREVEALED` status is invalid; content did not inform WS/GAP/H2 implementation, test thresholds, parameters, or code changes. Never reopen the holdout to quantify scope; require a separate remediation/replacement decision before `TRN-001` or training. | `PROJECT_PROGRESS.md`; `docs/project-brain/PROJECT_PROGRESS_DETAIL_20260828.md` | `PROJECT_CHARTER.md` |
| Legacy rollback capture boundary | First-deploy legacy capture previously omitted explicit `.worktrees` and release-control-plane exclusions, so a self-capture could recurse. **GUARDED**: fixed boundaries are excluded before descent; unknown symlink/reparse/special entries and unreadable directories fail closed with an actionable path rather than being followed or omitted; provenance remains `UNPROVEN`. | `tests/test_release_pipeline.py`; `docs/deployment/DEP_LEGACY_CAPTURE_RUNTIME_FAILURE_001_UPSTREAM_RESEARCH.md` | `docs/continuous_recorder.md` |
| Service generation continuity / stopped rollback | Candidate Recorder generation `21048` exited because `release_runner.py --component recorder` leaked its bootstrap argument to Recorder's no-argument CLI. A later SCM recovery generation then made the gate report a misleading parent-PID mismatch, and rollback could not start from `STOPPED/PID 0`. **GUARDED**: the runner isolates bootstrap arguments; every post-start proof stays bound to one SCM/WinSW PID and fails on loss/change; stopped recovery shares the same provenance gate without a redundant stop. | `tests/test_release_runner.py`, `tests/test_deployment_restart.py`; bounded evidence `DEP001-RETRY-20260829T205626433Z` | `docs/continuous_recorder.md` |

If no regression test is possible, record the reason beside the smallest
reproducible evidence artifact; do not substitute prose for an executable test.
