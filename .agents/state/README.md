# Loop state

`schema.json` and this README are tracked. Put actual run records under `runs/`; that directory
is ignored because loop state is noisy and environment-specific. Promote a run into durable task
history only by an explicit human decision.
