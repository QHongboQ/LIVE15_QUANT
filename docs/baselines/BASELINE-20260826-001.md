# LIVE15 Pre-Model-vNext Baseline — BASELINE-20260826-001

Read-only snapshot of runtime, storage, trainable pool, Dataset v1, and model-zoo state before any Model vNext work. The JSON file beside this report is the canonical compact identity.

## Runtime

- Supervisor `4256`: RUNNING.
- Official Recorder `2280`: HEALTHY, SDK-authoritative, legacy WS `LEGACY_ROLLBACK_ONLY`.
- Recorder health: 10/10 synchronized, unrecovered gaps 0, stale sources empty, fatal task/error absent, persistence progress fresh.
- Materializer `15140`: RUNNING, incremental/no-training mode; finalized evaluated 4,150, eligible 3,432, excluded 718, unevaluated 0, materialized rows 25,543.
- Paper `17536`: process RUNNING but trading state PAUSED by explicit `USER_PAUSE_TRADING`; no inference of a promoted model.
- Control Center `12408`: RUNNING; localhost API HTTP 200.
- Production writes: 0.

The separately reported SDK WS shadow process is non-authoritative and is not used to judge official Recorder health.

## Data and storage

The Recorder schema version is 10. The health snapshot reported 245,500,891 orderbook events, 1,404,772 prediction quotes, 16,459 lifecycle rows, 4,150 settlements, and 38,140 data-gap records (19,070 open and 19,070 recovered). The active HOT database was observed at 50,719,006,720 bytes with a 67,108,864-byte WAL and 13,991,936-byte SHM; cold archive bytes were 7,713,479,260 with 32,234 verified chunks and no failed chunks. The bounded storage audit classified growth as `STORAGE_GROWTH_HIGH_BUT_VALID`; this is an observation, not a retention or schema change.

## Frozen Dataset v1

`live15-dataset-v1-f81d7d1feebcbbaecff9` is immutable: 1,091 events, 7,984 rows, 42 features, nine decision offsets, 15-second source-age limits, and chronological event-grouped splits. Labels are finalized Kalshi settlement `yes`/`no` only. Dataset v1 coverage is 2026-08-20 through 2026-08-22 UTC.

## Model state

Model Zoo v1 final remains `NO_CHAMPION`; its documented blockers are that validation Brier did not beat the market-implied baseline, final-test net PnL was not positive after estimated costs, and final-test profit factor was not above one. The latest v2 zoo is a `FORWARD_CANDIDATE`, not a promoted champion. v3 is blocked with `INSUFFICIENT_MICROSTRUCTURE_EVIDENCE` and has no forward candidate. The Paper process is explicitly paused, so no active promotion is inferred from runtime status.

## Integrity gates

As-of timestamp, label, backfill, split, normalization-contract, and final-test integrity gates are PASS based on repository contracts and immutable manifests. Sequence readiness is `INSUFFICIENT_SEQUENCE_EVIDENCE`; this is a readiness limitation for vNext, not a failed data-integrity gate.

## Checker and immutability

An independent read-only checker pass verified the runtime status/API, `data/health.json`, Dataset v1 manifest, model manifests, and Git identity. Verdict: `PASS`. This artifact contains metadata/report only; no runtime process, database, historical artifact, model, dataset, or Production write was changed.
