# Issue tracker

LIVE15 uses the repository's GitHub Issues for work that needs durable public
tracking. Pull requests are review surfaces, not automatically triage inputs.

Use the GitHub integration/API or an approved host boundary for remote mutation;
never use sandbox Git HTTPS. Local task evidence may remain in the worktree until
it is ready for protected-main publication.
